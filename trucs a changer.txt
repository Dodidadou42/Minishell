!!!!!! DANS LE MAKEFILE J'AI MIT EN COMMENTAIRE LE REPETOIRE DE READLINE PCQ LE MIEN EST DIFFERENT, PENSE A REMETTRE

J'ai refait le heredoc pcq il marcher pas genre l'affichage etait bon et le systeme de signals aussi mais
tu fesais le pipe dans le process donc aucune chance de le recuperer en dehors, pareille pour l'assignation de fd_cmd
donc en gros apres l'entree etait forcement vide

J'ai revu le truc de pipe pour avoir les pipes qui se lancents en meme temps et qui sont wait en meme temps ensuite
donc la ca fait vraiment comme le shell avec genre wc | ls, cat /dev/urandom | cat, cat /dev/urandom | wc , etc
juste pour le cat ca fonctionne aussi mais je crois bien que faut faire une regle special pour cat pcq
dans bash faut juste appuyer sur entree et tu recupere le prompt et j'ai pas trouver d'autre commande qui fait la meme chose
donc encore a faire

J'ai vite fait modifier les signals pour que le truc de cat marche aussi avec les commandes dans le meme genre comme wc
et j'ai remagner vite fait pour avoir un seul fichier dcp

cat | cat --> OK

cat | cat | ls (-> testé dans la correction) 
--> Comme j'ai dit en haut, le ls s'affiche ensuite le cat se lance mais il faut faire un truc special pour lui

Quand on lance minishell depuis minishell il devient fou mais je pense c'est normal 

Gérer le fait qu'on peut créer des variable en mode : SALUT=wsh, puis export
--> Ca marche deja ca c'est quand il ya deja la variable faut modifier direct genre sans export apres, je vais m'en occuper


C'est a peu près tout ce qui me vient la


Cd
--------
cd // 					ca a l'air d'etre la meme chose que / mais juste avec // (donc /Users devient //Users)
						C'est good
cd /etc, cd /var.. 		faut voir sur les ordi de lecole mais moi ca m'envoie dans /private/var a la place de /var
						C'est good et c'était un enfer
unset HOME puis cd		faut mettre un message d'erreur ("minishell: cd: HOME not set" (exit code = 1)
						relou pcq si on unset HOME et cd on doit avoir ca mais si cd ~ ca doit marcher sans HOME,
						 pour l'instant ca segfault les deux, peux etre mettre char *root dans v au lancement comme ca meme si c'est unset
						 on l'aura toujours pour cd ~ faut voir

cd path | cmd			Le dossier ne dois pas changer si cd n'est pas la derniere commande utiliser (aucun pipe derriere)
						on peux utiliser v->pipe_count et v->pipe_start
						edit : faut aussi faire ca avec export donc je le ferai quand je ferai le truc de export plus haut

cd apres un delete du dossier parent

$OLDPWD					A chaque cd il faut set le repertoire acuelle dans la variable OLDPWD (Elle peux ne pas deja exister si il n'y a eu aucun cd)
						C'est good
Var. Env.
-----------
Variable $0 qui doit etre traiter comme $? mais ne change pas car toujours = minishell (facultatif je pense)
Actualiser $_="nom de la derniere commande" a chaque commande
Acutaliser $SHLVL= (avec 1 de base 2 si on rentre dans un script 3 si ce script en lance un autre etc)
					(je pense ont peux skip ca sera pas a la correction mais bon)

Export
-----------
export sans arguments doit print les valeurs pas encore export ou qui ont pas de valeurs je me suis compris
OLDPWD doit etre dans ces valeurs si il est pas encore set (lancement)
